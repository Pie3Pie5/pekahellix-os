# Test V0.4.5

1. Mettre à jour `index.html`, `style.css`, `script.js` et `sw.js` sur GitHub Pages.
2. Dans Cyber, sélectionner une réponse : l'affichage doit descendre automatiquement vers « Question suivante ».
3. Vérifier qu'une autre réponse peut toujours être sélectionnée avant validation.
4. Terminer le questionnaire avec au moins 80 %.
5. Vérifier l'apparition de « Transmettre mon score », saisir une adresse e-mail valide et toucher « Envoyer mon score ».
6. L'application e-mail doit s'ouvrir avec le destinataire, l'objet et le score préremplis. L'envoi reste volontaire et doit être confirmé dans l'application e-mail.
