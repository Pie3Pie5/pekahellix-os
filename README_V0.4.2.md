# Test V0.4.2

Sur GitHub, remplacer `index.html`, `style.css`, `script.js` et `sw.js`, puis valider le commit.
Après le déploiement GitHub Pages, ouvrir d’abord le site dans le navigateur du téléphone et actualiser. Fermer ensuite complètement la PWA et la relancer.

Vérifier :
1. la barre Temps / Communication / Cyber apparaît après connexion ;
2. elle reste visible dans chacun des trois modules ;
3. toucher un autre module depuis la barre change directement de module ;
4. Retour accueil ferme le module sans masquer la barre.
